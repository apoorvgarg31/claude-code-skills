# Pre-Commit Hook

Run before each commit to ensure quality.

## Checks
1. Lint code
2. Run quick tests
3. Check for secrets
4. Validate commit message

## Usage
```bash
cp hooks/pre-commit .git/hooks/
chmod +x .git/hooks/pre-commit
```
